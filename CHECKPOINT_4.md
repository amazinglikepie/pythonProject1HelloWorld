# Checkpoint 4 — capability data obtained; **the pair-set gate fails**. Stopping as instructed.

Access date 2026-09-13. Every number below is machine-generated by the scripts named, from files in this
repository. Source of the capability data: the models page you saved and uploaded
(`data/raw/artificial_analysis/models_page_2026-09-13.html`, 1,377,114 bytes,
sha256 `bb264e0557874cc9e739f333e850fb0c78c61b2f647ba7234aa12948e838979d`, upload commit `db07c23`
2026-09-13T02:08:16-07:00).

## Verdict

Your gate was: at least roughly 8 capability-matched pairs with price history spanning at least 12 months.
**Zero pairs can be formed.** Not 8, not 3. The reason is structural, and more downloading will not fix it.

## 1. What the saved page actually contains (`code/09_parse_aa_page.py`)

Three separate structures, with very different coverage:

| Structure | Rows | Has Intelligence Index? | Has price? |
|---|---|---|---|
| `initialModels` (the models selected in the chart when saved) | 26 | 25 of 26 | yes |
| JSON-LD chart datasets (18 charts) | 179 points | the index chart holds 20 models | 20 models |
| `models` registry | **646** | **no** | **no** |

Union of models with an Intelligence Index anywhere in the page: **25**. The page embeds scores only for the
models the chart was displaying (your "26 of 646" control) plus the chart's top-20. The 646-model registry
carries only slug, name, creator and release date.

The index is **v4.3**, described on the page as incorporating 10 evaluations (AA-Briefcase, GDPval-AA v2,
AutomationBench-AA, Terminal-Bench v4.0, SciCode, Humanity's Last Exam, GDP.pdf, CritPt, AA-Omniscience,
AA-LCR v1.1). Scores from different index versions are not comparable, which constrains any historical matching.

## 2. The overlap that decides the gate (`code/10_capability_overlap.py`)

| Bloc | Candidates with >= 365 d price history | Scored in the saved page | Scored in the Qiu daily top-10 | Listed in the 646 registry (score obtainable) |
|---|---|---|---|---|
| CN | 11 | **0** | **0** | 2 |
| US | 57 | **0** | **0** | 41 |

The two sets do not intersect at all, and the reason is visible in the dates:

- The 25 **scored** models were released between 2025-08-05 and 2026-09-10; **24 of 25 in 2026**. Their longest
  dated price history in the catalog is **95 days** (Claude Fable 5); most have 10 to 66 days.
- The 68 models **with 12+ months of price history** are 2023 to 2025 SKUs. None is scored in any data obtained.

So capability scores exist for models too new to have price history, and price history exists for models too old
to be scored here. That is the gate failure in one sentence.

## 3. A second, deeper obstacle: rolling API aliases

Even with unlimited capability data, the Chinese side has a problem the US side mostly does not. Chinese vendors
bill long-lived **rolling names** while replacing the model behind them; benchmarks score **fixed generations**.

| Bloc | Candidates | Fixed identity | Rolling alias |
|---|---|---|---|
| CN | 11 | 8 | 3 |
| US | 57 | 43 | 14 |

Evidence is in the catalog itself. `deepseek-chat` is priced continuously from 2024-05-07 (859 days) but its
advertised context window moves 32,000 to 128,000 to 131,072 and its price moves 0.14/0.28, then 0.6/1.7, then
0.28/0.42. That is at least three different models under one billing name. `deepseek-reasoner` does the same
(64,000 to 128,000). By contrast `gpt-4o-2024-11-20` and `claude-3-5-sonnet-20241022` are frozen SKUs.

Consequence: pairing `deepseek-chat` against a dated US snapshot would compare a moving target to a fixed one.
The pair would not be capability-matched at either end of the window, whatever score you attached to it.

Of the 11 Chinese candidates, those that are **both** fixed-identity **and** present in the Artificial Analysis
registry (so a score could in principle be obtained) number **2**: `deepseek-r1` and `deepseek-v3`.

## 4. A third obstacle: the price series barely move

The measurement you designed compares the change in the price ratio against the FX-implied cost change. For the
candidates that clear 12 months, there is almost no ratio change to explain.

Among the 11 Chinese candidates, price changes recorded over the whole window: `deepseek-r1` 0, `deepseek-v3` 0,
`qwen-plus` 0, `qwen-turbo` 0, `qwq-plus` 0, `kimi-k2-0711-preview` 0, `kimi-latest-128k` 0, `qwen-max` 1,
`moonshot-v1-128k` 1, `deepseek-chat` 1, `deepseek-reasoner` 1.

The second source agrees. Across its own daily captures, vendor-own models showing a single unchanged price level:
63 of 68 Chinese, 104 of 121 US. Alibaba's qwen-plus, qwen-turbo and qwq-plus are flat across 2025-09-15 to
2026-09-12 in both sources independently.

Caveat, stated rather than hidden: a flat line in a catalog can mean the vendor did not change the price **or**
that the catalog did not notice. For 2026 the catalog tracks verified changes within -1 to +4 days
(checkpoint 3), so flatness in 2026 is credible. For 2024 to 2025 there is no independent dated reference in hand,
so flatness in that period is unverified.

## 5. Exchange rate, for the record

Already prepared and usable (`data/fx/`): USD/CNY from 7.316 on 2023-09-06 to 6.7108 on 2026-09-04, a move of
**-8.27 percent** (yuan stronger, not weaker). Largest 30-day moves in the window: +2.34 percent weakening,
-2.51 percent strengthening.

Worth noting for your prior: over the usable window the yuan **strengthened**. The mechanism you described, where
a weaker yuan funds USD price cuts, would predict the opposite pressure over this period.

## 6. Options

**A. Stop here.** This is what your brief instructs, and what I have done. The data does not support the question
as specified.

**B. Change the question to one the data answers.** The strongest defensible finding already visible: for
fixed-identity Chinese SKUs with 12+ months of USD list-price history, USD list prices did not move at all while
USD/CNY moved 8 percent. That is close to zero FX pass-through into international list prices, and it supports
your prior. It cannot be expressed as 8 capability-matched pairs, and it rests on 2 to 8 Chinese models depending
on how strictly you require a capability score.

**C. Change the window instead of the question.** Restrict to 2026 models that do have scores. That yields
comfortably more than 8 capability-matched pairs, but with 10 to 95 days of price history each, which supports a
cross-section and a short-window comparison, not a 12-month trend, not the decomposition over sub-periods, and not
a 30/60/90-day lead-lag test.

**D. Collect more capability data first.** You would re-save the models page with all 646 models selected in the
chart, which should embed scores for the older fixed models. This would raise the Chinese side from 0 scored to
perhaps 2 to 4, because the Qwen and Kimi long-history SKUs are rolling names that no fixed-generation score
matches, and because GLM (285 days), MiniMax (264) and Doubao (241) simply do not reach 12 months in any source.
It will not reach 8.

My recommendation is **B**, with **D** run first if you want the 2 to 4 Chinese models carrying a verified score
rather than none. I have not done either; the brief says stop at the gate, so I stopped.

## 7. What exists in the repository right now

Verified and reusable regardless of which option you choose: three cross-checked price sources with commit-level
provenance, 674 integrity-verified catalog versions spanning 2023-09-06 to 2026-09-13, a full FX series with
rolling-change distributions, per-model timelines with identity classification, and every filtering decision
logged with a reason and a count.
